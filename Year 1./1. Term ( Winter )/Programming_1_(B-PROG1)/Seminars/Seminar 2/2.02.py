# 2. Vytvorte skript, ktory:
# A. Definuje funkciu priemer_troch, ktora pre vstupy a,b,c vrati hodnotu (a+b+c)/3 (a,b,c budu parametrami funkcie).
# B. Pomocou funkcie priemer_troch vypise priemer cisel 5,6,7.

def priemer_troch(a, b, c):
    return (a + b + c) / 3


print(priemer_troch(5, 6, 7))
